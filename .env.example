# Local dev
DATABASE_URI=file:./terminal-schedule.db
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Production (Docker): DATABASE_URI & MEDIA_DIR are set in docker-compose.yml.
# Only PAYLOAD_SECRET is required in .env. Optionally set your public URL:
# NEXT_PUBLIC_SERVER_URL=https://jadwal.terminalkutoarjo.id
